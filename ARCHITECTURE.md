# 🏗️ System Architecture

## 📐 High-Level Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     CLIENT LAYER                             │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────┐         ┌──────────────────┐          │
│  │  Customer Site   │         │   Admin Panel    │          │
│  │                  │         │                  │          │
│  │  - Index.html    │         │  - Admin.html    │          │
│  │  - menu.html     │         │  - Dashboard     │          │
│  │  - Order.html    │         │  - Products      │          │
│  │  - Contact.html  │         │  - Menus         │          │
│  │                  │         │  - Orders        │          │
│  └────────┬─────────┘         └────────┬─────────┘          │
│           │                            │                     │
│           └────────────┬───────────────┘                     │
│                        │                                     │
└────────────────────────┼─────────────────────────────────────┘
                         │
                         │ HTTP/AJAX Requests
                         │
┌────────────────────────▼─────────────────────────────────────┐
│                   APPLICATION LAYER                          │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│                    Flask REST API (app.py)                   │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              API Endpoints                           │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  Products    │  Menus  │  Orders  │  Customers      │    │
│  │  - GET       │  - GET  │  - GET   │  - GET          │    │
│  │  - POST      │  - POST │  - POST  │                 │    │
│  │  - PUT       │  - PUT  │  - PUT   │                 │    │
│  │  - DELETE    │  - DELETE│         │                 │    │
│  └──────────────┴─────────┴──────────┴─────────────────┘    │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │           Business Logic Layer                       │    │
│  ├─────────────────────────────────────────────────────┤    │
│  │  - Data Validation                                   │    │
│  │  - Error Handling                                    │    │
│  │  - Data Serialization                                │    │
│  │  - File Upload Management                            │    │
│  │  - Order Calculation                                 │    │
│  └─────────────────────────────────────────────────────┘    │
│                                                               │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ SQL Queries
                         │
┌────────────────────────▼─────────────────────────────────────┐
│                    DATA LAYER                                │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│                    MariaDB Database                          │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │   products   │  │    menus     │  │   orders     │      │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤      │
│  │ id           │  │ id           │  │ id           │      │
│  │ name         │  │ name         │  │ customer_id  │      │
│  │ category     │  │ description  │  │ total_amount │      │
│  │ description  │  │ is_visible   │  │ status       │      │
│  │ price        │  │ category     │  │ order_date   │      │
│  │ image_url    │  │ image_url    │  └──────────────┘      │
│  │ created_at   │  │ created_at   │                         │
│  └──────────────┘  └──────────────┘                         │
│                                                               │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │  customers   │  │ order_items  │  │menu_products │      │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤      │
│  │ id           │  │ id           │  │ menu_id      │      │
│  │ name         │  │ order_id     │  │ product_id   │      │
│  │ email        │  │ product_id   │  └──────────────┘      │
│  │ phone        │  │ quantity     │                         │
│  │ created_at   │  │ price        │                         │
│  └──────────────┘  └──────────────┘                         │
│                                                               │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow

### Customer Order Flow
```
1. Customer browses menu (menu.html)
   ↓
2. JavaScript fetches products (GET /api/products)
   ↓
3. Customer adds items to cart (localStorage)
   ↓
4. Customer places order (Order.html)
   ↓
5. JavaScript sends order (POST /api/orders)
   ↓
6. Backend validates and creates order
   ↓
7. Database stores order and items
   ↓
8. Success response sent to customer
```

### Admin Product Management Flow
```
1. Admin opens product page (Admin.html)
   ↓
2. JavaScript fetches products (GET /api/products)
   ↓
3. Admin creates/edits product with image
   ↓
4. Form data sent (POST/PUT /api/products)
   ↓
5. Backend validates and saves image
   ↓
6. Database stores product info
   ↓
7. Success response updates UI
```

---

## 📊 Database Schema

### Entity Relationship Diagram
```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│  customers  │         │   orders    │         │order_items  │
├─────────────┤         ├─────────────┤         ├─────────────┤
│ id (PK)     │◄───────┤customer_id  │         │ id (PK)     │
│ name        │         │ id (PK)     │◄───────┤ order_id    │
│ email       │         │ total_amount│         │ product_id  │
│ phone       │         │ status      │         │ quantity    │
│ created_at  │         │ order_date  │         │ price       │
└─────────────┘         └─────────────┘         └──────┬──────┘
                                                        │
                                                        │
                        ┌─────────────┐                │
                        │  products   │                │
                        ├─────────────┤                │
                        │ id (PK)     │◄───────────────┘
                        │ name        │
                        │ category    │         ┌─────────────┐
                        │ description │         │menu_products│
                        │ price       │         ├─────────────┤
                        │ image_url   │◄───────┤ product_id  │
                        │ created_at  │         │ menu_id     │
                        └─────────────┘         └──────┬──────┘
                                                        │
                                                        │
                                                ┌───────▼─────┐
                                                │    menus    │
                                                ├─────────────┤
                                                │ id (PK)     │
                                                │ name        │
                                                │ description │
                                                │ is_visible  │
                                                │ category    │
                                                │ image_url   │
                                                │ created_at  │
                                                └─────────────┘
```

---

## 🔌 API Architecture

### RESTful API Design
```
/api
├── /products
│   ├── GET     - List all products
│   ├── POST    - Create product (multipart/form-data)
│   ├── PUT     - Update product (multipart/form-data)
│   └── DELETE  - Delete product
│
├── /menus
│   ├── GET     - List all menus with products
│   ├── POST    - Create menu
│   ├── PUT     - Update menu
│   └── DELETE  - Delete menu
│
├── /orders
│   ├── GET     - List all orders with details
│   ├── POST    - Create new order (JSON)
│   └── PUT     - Update order status (JSON)
│
├── /customers
│   └── GET     - List all customers with stats
│
├── /dashboard
│   └── /stats  - Get dashboard statistics
│
└── /health
    └── GET     - Health check
```

---

## 🛡️ Security Architecture

### Security Layers
```
┌─────────────────────────────────────────┐
│         Input Validation Layer          │
│  - Required field checks                │
│  - Type validation                      │
│  - Safe type conversion                 │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│      File Upload Security Layer         │
│  - Extension validation                 │
│  - Filename sanitization                │
│  - Size limits (16MB)                   │
│  - Secure file storage                  │
└────────────────┬────────────────────────┘
                 │
┌────────────────▼────────────────────────┐
│       Database Security Layer           │
│  - Parameterized queries                │
│  - No SQL injection                     │
│  - Connection pooling                   │
│  - Proper error handling                │
└─────────────────────────────────────────┘
```

---

## 📦 File Structure

```
Backend/
│
├── app.py                      # Main application
│   ├── Configuration
│   ├── Database Functions
│   ├── Helper Functions
│   ├── API Routes
│   └── Application Entry
│
├── static/
│   ├── Admin.html             # Admin interface
│   └── uploads/               # Uploaded images
│
├── js/
│   ├── admin.js               # Admin logic
│   ├── menu.js                # Menu page logic
│   ├── order.js               # Order logic
│   ├── contact.js             # Contact logic
│   └── script.js              # Common functions
│
├── *.html                     # Customer pages
│   ├── Index.html
│   ├── menu.html
│   ├── Order.html
│   └── Contact Us.html
│
├── *.css                      # Stylesheets
│
├── test_api.py                # Testing suite
├── requirements.txt           # Dependencies
├── start.bat                  # Startup script
│
└── Documentation/
    ├── README.md
    ├── QUICK_START.md
    ├── CHANGELOG.md
    ├── IMPROVEMENTS_SUMMARY.md
    └── ARCHITECTURE.md (this file)
```

---

## 🔧 Technology Stack Details

### Backend Stack
```
Python 3.8+
├── Flask 3.0.0          # Web framework
├── Flask-CORS 4.0.0     # CORS handling
├── MariaDB 1.1.8        # Database connector
└── Werkzeug 3.0.1       # WSGI utilities
```

### Frontend Stack
```
HTML5
├── Tailwind CSS         # Styling framework
├── Vanilla JavaScript   # No framework
└── Material Icons       # Icon library
```

### Database
```
MariaDB/MySQL
└── InnoDB Engine        # Transaction support
```

---

## 🚀 Deployment Architecture

### Development
```
localhost:5000
├── Flask Development Server
├── Debug Mode: ON
└── Auto-reload: ON
```

### Production (Recommended)
```
Production Server
├── Gunicorn/uWSGI       # WSGI server
├── Nginx                # Reverse proxy
├── SSL/TLS              # HTTPS
└── Environment Variables # Configuration
```

---

## 📈 Scalability Considerations

### Current Architecture
- Single server deployment
- Direct database connections
- File-based image storage

### Future Scalability
```
┌─────────────┐
│Load Balancer│
└──────┬──────┘
       │
   ┌───┴───┬───────┬───────┐
   │       │       │       │
┌──▼──┐ ┌──▼──┐ ┌──▼──┐ ┌──▼──┐
│App 1│ │App 2│ │App 3│ │App N│
└──┬──┘ └──┬──┘ └──┬──┘ └──┬──┘
   │       │       │       │
   └───┬───┴───────┴───────┘
       │
   ┌───▼────┐
   │Database│
   │Cluster │
   └────────┘
```

---

**This architecture provides a solid foundation for a restaurant management system with room for growth and enhancement.**

